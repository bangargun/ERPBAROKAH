{
  "name": "mris-multi-restaurant-finance",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "sync-src": "rsync -av --delete web_admin/src/ src/",
    "sync-dist": "rsync -av --delete web_admin/dist/ dist/",
    "build:admin": "npm run sync-src && cd web_admin && npm run build && cd .. && npm run sync-dist",
    "build:pos": "npm run sync-src && vite build",
    "build": "npm run sync-src && cd web_admin && npm run build && cd .. && npm run sync-dist",
    "preview": "vite preview",
    "server": "node server.js",
    "start": "node server.js",
    "deploy": "npm run build && git add -A && git commit -m 'deploy: update build' --allow-empty && git push && sshpass -p 'Barokahgrub30@@' ssh -o StrictHostKeyChecking=no root@187.77.122.142 'cd /var/www/erp-barokah && git fetch origin && git reset --hard origin/main && pm2 restart erp-barokah'"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "better-sqlite3": "^11.8.0",
    "canvas-confetti": "^1.9.4",
    "chart.js": "^4.5.1",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "lucide-react": "^0.475.0",
    "mysql2": "^3.12.0",
    "react": "^19.0.0",
    "react-chartjs-2": "^5.3.1",
    "react-dom": "^19.0.0",
    "recharts": "^2.15.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/react": "^19.0.8",
    "@types/react-dom": "^19.0.3",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.1.0"
  }
}
